export enum ORDER_STATUS {
    PROCESSED = "processed",
    PAID = "paid",
    UNPAID = "un-paid",
    DELIVERED = "delivered",
    SHIPPED = "shipped",
    CANCELLED = "cancelled",
}

export enum ORDER_TYPE {
    RETORT = "retort",
    SAMPLE_ORDER = "sample-order",
    CONTRACT = "contract",
}