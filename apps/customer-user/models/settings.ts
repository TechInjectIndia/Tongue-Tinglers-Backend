const { Op } = require("sequelize");
import {
    TSettings,
    TPayloadSettings
} from "../../../types";
import { UserModel } from "../../../database/schema";

export class CustomerSettingsRepo {
    constructor() { }

}
