export enum INQUIRY_TYPE {
    CUSTOMER = "customer",
    FRANCHISE = "franchise",
}

export enum CAMPAIGN_STATUS {
    DRAFT = "draft",
    SENT = "sent",
    ARCHIVED = "archived",
}

export enum EMAIL_STATUS {
    DELIVERED = "delivered",
    FAILED = "failed",
    DRAFT = "draft"
}