export enum ORDER_STATUS {
    PROCESSED = "processed",
    DELIVERED = "delivered",
    SHIPPED = "shipped",
    CANCELLED = "cancelled",
}

export enum PAYMENT_STATUS {
    PAID = "paid",
    UNPAID = "un-paid",
    PROCESSED = "processed",
}

export enum ORDER_TYPE {
    RETORT = "retort",
    SAMPLE_ORDER = "sample-order",
    CONTRACT = "contract",
}