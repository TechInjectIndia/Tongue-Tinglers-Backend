export interface PetPoojaBasePayload {
    // Application key for authentication
    app_key: string;

    // Application secret for authentication
    app_secret: string;

    // Access token for authorization
    access_token: string;
}